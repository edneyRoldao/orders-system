import { AppUtils } from './utils/app.utils'

const r = AppUtils.sum(10, 11, 5)
console.log(r);

// const r = AppUtils.sum(-1, 11, 5)
// console.log(r);

// const r = AppUtils.sum(true, 11, 5)
// console.log(r);
